# handling form event and input

# This is used for element like select, input and other html tags React.changeEvent<HTML__tag___Element>

# to handle the form we use React.FormEvent<HTMLFormElement>
# the events type for a form submission